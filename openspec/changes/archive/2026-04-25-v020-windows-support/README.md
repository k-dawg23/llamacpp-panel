# v020-windows-support

OpenSpec change for release **v0.2.0**: initial **Windows 10 / 11** support (executable resolution, `PATH` for DLLs, validation without POSIX `X_OK`), POSIX unchanged. **macOS out of scope.**

Artifacts: `proposal.md`, `design.md`, `tasks.md`, `specs/server-supervisor/spec.md`, `specs/gpu-devices/spec.md`.
